[package]
name = "prophet-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
prophet-dsp = { path = "../prophet-dsp" }
dsp-common = { path = "../dsp-common" }

[profile.release]
opt-level = 3
lto = true
